import React from "react";
import { TrackCard } from "../components/TrackCard/TrackCard";
interface Props {}

export const Tracks = (props: Props) => {
	return (
		<div>
			<TrackCard />
		</div>
	);
};
